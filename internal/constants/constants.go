// Package constants Holds the Values the Program means everywhere.
package constants

// UserAgent Identifies this Service to Card Sources; it does not Vary by
// Environment.
const UserAgent = "muchi-api/1.0"
